---
title: "Settings reference"
description: "Every setting at every level, its key, its default, and which tier honours it"
icon: "list-check"
---

import { Pro } from '/snippets/pro.mdx';
import { Media } from '/snippets/media.mdx';

Settings live at three levels. A preset-level setting overrides the store-wide one. An item-level setting applies to one line.

All store settings live in a single option, `cartpresets_settings`. Preset and item settings live in their own tables.

<Media>The Settings screen with the sidebar System status card visible</Media>

## Store settings

The sections below are in the order the screen draws them: Licence<Pro />, Carrier product, Cart behaviour, Usage limits, Messages and labels, Link settings, Menu location, Data management.

### Licence<Pro />

On Pro the first card on Settings is **Licence**, above the carrier product. It holds the key, its status, and the Activate, Sync and Deactivate buttons. Those buttons act straight away and sit outside the save bar, so there's nothing to save. The licence has no stored setting keys in `cartpresets_settings`. See [Licence and updates](/cartpresets/licence).

### Carrier product

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Carrier product | `carrier_product_id` | `0` (none) | Both |

The hidden product custom items ride on. See [The carrier product](/cartpresets/settings/carrier-product).

### Cart behaviour

Four blocks, in this order: what happens to the cart a link finds, what happens when an item can't be added, where a customer lands when the link works, and where they land when it can't be used at all.

**Existing cart**

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Clear the existing cart | `clear_cart` | Off | Both |
| Allow duplicate presets | `allow_duplicate` | Off | Both |
| Keep existing items | `keep_existing` | On | Both |

The lower two are disabled while **Clear the existing cart** is on. An emptied cart has no earlier copy to keep and nothing of the customer's to spare.

**Unavailable items**

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Unavailable items | `invalid_item` | `skip` | Both |

`invalid_item` accepts `skip` (**Skip the item**) or `none` (**Refuse the preset**).

**Redirect after adding**

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Redirect after adding | `post_add` | `cart` | Both |
| Custom URL | `post_add_url` | Empty | Both |

`post_add` accepts `cart`, `checkout`, `stay` or `custom`. A `custom` destination won't save without an address.

**Unavailable links**

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Unavailable links | `refused_action` | `cart` | Both |
| Custom link | `refused_url` | Empty | Both |
| Show an error message | `refused_notice` | On | Both |

`refused_action` accepts `cart` or `custom`. One answer covers every way a link can be refused: expired, sold out, switched off, restricted, used up, or matching no preset at all. See [Where customers land](/cartpresets/links/destinations).

<Note>
  This is store-wide and editable in **both versions**. Only the per-preset override needs Pro. Where a refused customer lands can't put a link back on sale, so there's nothing for a tier gate to protect.
</Note>

### Usage limits

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Count orders with these statuses | `redemption_statuses` | on-hold, processing, completed | Pro to edit, honoured on both |

A slot is taken when an order enters one of these, and given back when it leaves them, so a cancelled order doesn't permanently eat part of a limited run. See [Purchase limits](/cartpresets/building/purchase-limits).

### Messages and labels

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| All twenty-one strings | `notices` | Empty (shipped wording) | Both |

Twenty-one strings, drawn as twenty-two editable fields: the per-customer cap message has a singular and a plural form. Only the ones you've rewritten are stored. See [Cart notices and wording](/cartpresets/links/wording).

### Link settings

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| URL parameter | `url_param` | `preset` | Both |
| Pretty permalink base | `permalink_base` | `/preset/` | Both |
| Append the preset name as a UTM campaign | `utm` | Off | Both |

Changing either URL setting breaks links you've already shared.

### Menu location

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Where you reach this panel from | `menu_location` | `own` | Both |

`own` (**Top-level menu**) is a **Cart Presets** menu with All Presets, Add New Preset and Settings beneath it. `products` (**Under Products**) is a single **Cart Presets** entry under **Products**, with no submenu. Add New and Settings stay reachable from the listing screen. Saving this reloads the page to move the menu.

### Data management

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Presets and their settings | `delete_links` | Off | Both |
| Store settings, wording and licence | `delete_settings` | Off | Both |
| The carrier product | `delete_carrier` | Off | Both |

Whatever you select is permanently deleted on uninstall. Deactivating never removes anything. `delete_settings` covers more than its wording says: the whole settings option, your licence record, each administrator's listing preferences, and cached preset data.

The section also carries **Preset data → Export presets / Import presets**<Pro /> — see [Import and export](/cartpresets/import-export).

See [Data and uninstall](/cartpresets/settings/data-and-uninstall) and [Privacy and personal data](/cartpresets/settings/privacy).

## Preset settings

Set in the editor, on one preset.

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Date (publish) | `created_at` | Now | Pro to edit |
| Preset ends on | `end_at` | Empty | Pro to edit |
| Maximum orders | `max_total` | Empty | Pro to edit, honoured on both |
| Maximum orders per customer | `max_per` | `0` | Pro to edit, honoured on both |
| Redirect after adding | `redirect` | `global` | Both |
| Custom URL | `custom_url` | Empty | Both |
| Use the Settings default (existing cart) | `cart_override` | Off (follow Settings) | Both |
| Clear the existing cart | `clear_cart` | Off | Both |
| Allow duplicate presets | `allow_duplicate` | Off | Both |
| Keep existing items | `keep_existing` | On | Both |
| Coupon code | `coupon` | Empty | Pro — not applied on free |
| Signed-in customers only | `logged_in` | Off | Pro — not enforced on free |
| Limit to roles | `roles` | Empty | Pro — not enforced on free |
| Unavailable links | `refused_action` | `global` | Pro to edit, honoured on both |
| Custom link | `refused_url` | Empty | Pro to edit, honoured on both |
| Show an error message | `refused_notice` | On | Pro to edit, honoured on both |

`redirect` and `refused_action` both accept `global` (**Use the Settings default**). `refused_action` otherwise accepts `cart` or `custom`, the same two the store offers.

The three cart switches are only read while `cart_override` is on. With it off, the store's own answers apply and the editor draws them disabled — see [Cart behaviour](/cartpresets/links/cart-behaviour#per-preset-override).

Watch the polarity here. The switch is labelled **Use the Settings default** and it ships **on**, which is `cart_override` set to off. Turning that switch off is what sets `cart_override` on and hands the preset its own answers.

**A role list implies the sign-in switch.** A guest holds no roles. Roles without `logged_in` would refuse every visitor while the editor showed no sign-in requirement. So saving a role list turns `logged_in` on, whether it arrives from the editor, an import or a filter.

<Note>
  There's no per-preset rejection message. A visitor who is turned away reads the store-wide wording: `not_signed_in` and `role_not_allowed` in **Messages and labels**.
</Note>

## Item settings

Set on one item inside a preset.

| Setting | Key | Default | Tier |
| --- | --- | --- | --- |
| Discount type | `disc_type` | None | Both |
| Discount value | `disc_value` | `0` | Both |
| Use the regular price instead of the sale price | `use_regular` | Off | Both |
| Payments a discount covers | `sub_payments` | `0` (all) | Pro — free reads as first only |
| Waive the sign-up fee | `sub_waive_fee` | Off | Pro — free charges the fee |
| Allow coupon discounts on this item | `allow_coupons` | Off | Both |
| Let the customer change the quantity in the cart | `allow_qty` | Off | Both |
| Minimum quantity | `qty_min` | `1` | Both |
| Maximum quantity | `qty_max` | `0` (no limit) | Both |
| Customer can remove this item | `removable` | Off | Both |
| Variation attributes | `variation_attributes` | Empty | Both |
| Custom item name | `custom_name` | Empty | Pro |
| Custom item price | `custom_price` | `0` | Pro |
| Description | `description` | Empty | Pro |
| Image | `image_id` | `0` | Pro |
| Needs shipping | `requires_shipping` | Off | Pro |
| Weight, length, width, height | `weight`, `length`, `width`, `height` | Empty | Pro |
| Shipping class | `shipping_class_id` | `0` | Pro |

`disc_type` accepts `none`, `pct`, `fixed` or `custom`.

## What "Pro to edit, honoured on both" means

Some settings can only be *set* on Pro, but are *enforced* in both versions.

Purchase limits are the clearest case. Setting a cap needs Pro. Both versions count claims against a cap and refuse a link once it fills, because moving to the free version must never put a sold-out link back on sale.

The same logic covers schedules, and a preset's own **Unavailable links** answer: moving to the free version must not quietly send a customer somewhere other than where the merchant chose. The rule: **honour the restriction, drop the enhancement.**

## Where settings are stored

| Level | Storage |
| --- | --- |
| Store | One option, `cartpresets_settings` |
| Preset | Columns and a settings blob on `cartpresets_presets` |
| Item | A settings blob on `cartpresets_items` |

Writes walk the schema, not the request, so a key that isn't in the schema is never saved, no matter what a request contains. Developers can register their own keys — see [the hooks reference](/cartpresets/developers/hooks-reference#schema).
